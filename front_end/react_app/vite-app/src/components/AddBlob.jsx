import React, { useState } from "react";
import axios from "axios";

export function PostFileBlob(userListOfBlobs) {
  // given already format checked list of blobs
  // send post to backend
  // possibly send back status
}
